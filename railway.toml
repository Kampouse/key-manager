[build]
buildCommand = "cd fastkv-server && cargo build --release"
builder = "RAILPACK"

[deploy]
startCommand = "cd fastkv-server && ./target/release/fastkv-server"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
